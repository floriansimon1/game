#pragma once

#include <cmath>

namespace game::math {
    constexpr float halfPeriod     = static_cast<float>(M_PI);
    constexpr float quarterPeriod  = static_cast<float>(M_PI) / 2.f;
    constexpr float completePeriod = 2.f * static_cast<float>(M_PI);
}
